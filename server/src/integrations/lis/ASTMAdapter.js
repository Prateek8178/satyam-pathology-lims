const LISAdapter = require('./LISAdapter');
class ASTMAdapter extends LISAdapter {
  constructor(config) { super(config); throw new Error('[ASTMAdapter] Not yet configured.'); }
}
module.exports = ASTMAdapter;
